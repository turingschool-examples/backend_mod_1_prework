# prints "I will not count my chickens"
puts "I will now count my chickens:"

# completes the math functions in #{} and returns an integer
puts "Hens #{25.0 + 30.0 / 6.0}"
puts "Roosters #{100.0 - 25.0 * 3.0 % 4.0}"

# prints "Now I will count the eggs:"
puts "Now I will count the eggs:"

# completes the math and returns an integer
# does not need to be in #{} since the inegers are not inside a string
puts 3.0 + 2.0 + 1.0 - 5.0 + 4.0 % 2.0 - 1.0 / 4.0 + 6.0

# prints the text and does not complete the math since this is only a string
puts "Is it true that 3.0 + 2.0 < 5.0 - 7.0?"

# completes the math and returns an integer
# does not need to be in #{} since the inegers are not inside a string
puts 3.0 + 2.0 < 5.0 - 7.0

# completes the math functions in #{} and returns an integer

puts "What is 3 + 2? #{3.0 + 2.0}"
puts "What is 5 - 7? #{5.0 - 7.0}"

# prints "Oh, that's why it's false."
puts "Oh, that's why it's false."

# prints "How about some more."
puts "How about some more."

# completes the math functions in #{} and returns an integer
puts "Is it greater? #{5.0 > -2.0}"
puts "Is it greater or equal? #{5.0 >= -2.0}"
puts "Is it less or equal? #{5.0 <= -2.0}"
