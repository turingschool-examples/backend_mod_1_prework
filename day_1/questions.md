## Day 1 Questions

1. How would you print the string `"Hello World!"` to the terminal?

```
- puts "Hello World!"
```

1. What character is used to indicate comments in a ruby file?

```
# This pound sign is an indicator of a comment
```

1. Explain the difference between an integer and a float?

- An integer is a number that is whole while a float is a number that calculates out to a decimal

1. In the space below, create a variable `animal` that holds the string `"zebra"`

```
animal = "zebra"
```

1. How would you print the string `"zebra"` using the variable that you created above?

```
puts animal
```

1. What is interpolation? Use interpolation to print a sentence using the variable `animal`.

```
# Interpolation is the use of a variable inside the calling of another string or method
puts "I can't believe a #{animal} is actually a donkey and not a horse!"
```

1. What method is used to get input from a user?

```
gets.chomp
```

1. Name and describe two common string methods:

```
# This is a common string command that returns a string capitalized
str.capitalize

# This string method takes a string and deletes specific characters from it
str.delete(example)
```
