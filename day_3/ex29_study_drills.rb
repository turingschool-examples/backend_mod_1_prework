# 1) the "if" statement allows to execute a piece of code under the condition that a specific requirement is met
# 2) Indenting the code allows the reader to see more clearly what part of the code will be executed if the condition is mnet
# 3) without indentation, the code executes without error.
# 4) if "blue" == "red"
#      puts "what's happening?"
#    end
# 5) if we change the initial values for `people`, `cats` and `dogs` the code produces different messages.
