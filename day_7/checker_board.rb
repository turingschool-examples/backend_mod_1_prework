# I attempted this one. I'll probably submit and then make a few more attempts at this.
# I don't want my struggling with this to prevent me from submitting, so I'll come back to this.

puts "How many squares in a row?"
n = gets.chomp.to_i

class Board
  def initialize(rows, columns)
    @rows = rows
    @columns = columns
  end

  def board_setup()
  end
end
