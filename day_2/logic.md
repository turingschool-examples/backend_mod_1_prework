### The following terms are used to determine if something is "true" or "false"

`&&` and

`||` or

`!` `not` not

`!=` not equal

`==` equal

`>=` greater-than-equal

`<=` less-than-equal

`true`

`false`
