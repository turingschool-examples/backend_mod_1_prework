<!--
.last: convenience method to fetch last element of an array

.sort: rearrange the order of the elements
    --strings: alphabetical return
    --numbers: ascending value return

.each: iterate through an element (adding something in between each string)

.join: mash elements together into one string

.index: find the address of a specific element inside the array

.include: Ask an array if an element is present (boolean)

.collect: Add something to each element in array, using block notation.

.first: returns first element

.last: returns last element

.shuffle: shuffle the elements (but not permanent shuffle) -->
