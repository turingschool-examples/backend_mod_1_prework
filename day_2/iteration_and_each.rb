numbers = [1, 2, 3, 4]
numbers.each do |numbers|
  puts numbers
end

puts numbers * 2

num = [1, 2, 3, 4,]
num.each {|num| puts num}

names = ["Megan", "Brian", "Sal"]
names.each do |names|
    puts names
end

puts names [0]

# Exercises
#after our block we can do puts and multiply to print our variables repeatedly.
# we can do puts [] and define the specific elements we want printed out. even or odd
# within our block if we * 2 all outputs will be multiplied
# to print out
# print out first and last we can use .first and .last
# can use .index to know how many characters are in the element
