#Prints a string of text to the screen.
puts "I will now count my chickens:"

#Prints "Hens" to the screen and performs computation. Inserts the result of the computation instead of the initial characters.
puts "Hens #{25 + 30 / 6}"
#Prings "Roosters" to the screen and performs computation. Inserts the result of the computation rather than the initial characters.
puts "Roosters #{(100 - ((25 * 3) % 4).to_f)}"

#Prints to the screen "Now I will count the eggs:"
puts "Now I will count the eggs:"

#Prints to the screen the result of computation
puts (3 + 2 + 1 - 5 + 4 % 2 - 1 / 4 + 6).to_f

#Prints the string to the screen.
puts "Is it true that 3 + 2 < 5 - 7?"

#performs the computation of whether 5 < -2. Results in False, which prints to the screen.
puts 3 + 2 < 5 - 7

#Prints to the screen "What is .....?" Performs the computation and inserts the result.
puts "What is 3 + 2? #{3 + 2}"
puts "What is 5 - 7? #{5 - 7}"

#Prints to the screen "Oh, that's why it's false."
puts "Oh, that's why it's false."

#Prints to the screen "How about some more."
puts "How about some more."

#Prints to the screen "Is it ......?" Performs the computation and returns a true or false value.
puts "Is it greater? #{5 > -2}"
puts "Is it greater or equal? #{5 >= -2}"
puts "Is it less or equal? #{5 <= -2}"
